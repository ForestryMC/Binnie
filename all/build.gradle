archivesBaseName = 'all-' + mcversion

dependencies {
	runtime project(":botany")
	runtime project(":core")
	runtime project(":extrabees")
	runtime project(":extratrees")
	runtime project(":genetics")
}
